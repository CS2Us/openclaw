{
  "name": "@openclaw/chromite-bridge",
  "version": "0.1.0",
  "description": "Bridge OpenClaw chat DMs and /chromite commands into a client-side AI-commerce agent loop against local chromite-server.",
  "type": "module",
  "dependencies": {
    "@openclaw/chromite-native": "workspace:*"
  },
  "devDependencies": {
    "@openclaw/plugin-sdk": "workspace:*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
