{
  "name": "@openclaw/agent-bridge",
  "version": "0.1.0",
  "description": "Bridge OpenClaw chat /agent commands to explicit local provider adapters.",
  "type": "module",
  "devDependencies": {
    "@openclaw/plugin-sdk": "workspace:*"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ],
    "build": {
      "staticAssets": [
        {
          "source": "scripts/perm-hook.cjs",
          "output": "scripts/perm-hook.cjs"
        }
      ]
    }
  }
}
